package Logic;

import java.util.Vector;

public class BoardLogic {











    /*void BoardBuilder(){
        for (int x = 0; x < 8; x++){
            for (int y = 0; y < 8; y++) {
                boolean isWhiteSquare = (y + x) % 2 != 0;

                var squareColor = (isWhiteSquare) ? whiteCol : blackCol;
                //var position = new Vector<> (-3.5f + x, 3.5f + y);
            }
        }
    }

     */
}
